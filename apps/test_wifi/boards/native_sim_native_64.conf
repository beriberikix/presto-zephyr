# No Wi-Fi hardware on native_sim — drop the entire net/wifi stack so the
# binary still smokes cleanly.
CONFIG_NETWORKING=n
CONFIG_NET_L2_WIFI_MGMT=n
CONFIG_WIFI=n
CONFIG_NET_MGMT=n
CONFIG_HEAP_MEM_POOL_SIZE=0
