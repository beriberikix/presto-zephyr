# Run the LVGL test on the host via Zephyr's SDL display emulator. Mouse
# clicks arrive through the input_sdl_touch node, so the widgets are fully
# interactive in the SDL window.

CONFIG_SDL_DISPLAY=y
